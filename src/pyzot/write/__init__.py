"""pyzot.write — write capability via Zotero's connector HTTP server.

All writes go through ConnectorClient; the SQLite handle stays read-only.
Requires the optional 'write' extra: pip install "pyzot[write]"
"""
