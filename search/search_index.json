{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to pyzot","text":"<p>pyzot (<code>zot</code>) is a read-only Python CLI and SDK for your local Zotero library. It queries the SQLite database directly \u2014 no Zotero app or API key needed.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Blazing Fast: Directly queries <code>zotero.sqlite</code>.</li> <li>Read-Only: Safe to use. Never writes to the database.</li> <li>Python SDK: Rich programmatic access to your Zotero library via Pydantic models.</li> <li>CLI Tool: Powerful <code>zot</code> command-line interface for browsing, searching, and exporting.</li> <li>Full-text Retrieval Flow: Local cache, direct network retrieval, and metadata fallback.</li> </ul>"},{"location":"#project-layout","title":"Project Layout","text":"<ul> <li><code>src/pyzot/db.py</code>: Database connection and auto-discovery.</li> <li><code>src/pyzot/models.py</code>: Pydantic models mapping Zotero entries.</li> <li><code>src/pyzot/queries/</code>: SQL queries to search and retrieve data.</li> <li><code>src/pyzot/cli/</code>: Click-based CLI application.</li> </ul> <p>Navigate to Getting Started to begin.</p>"},{"location":"api-reference/","title":"API Reference","text":"<p>Browse the programmatic API for <code>pyzot</code>. This SDK provides safe, read-only database connections and query helpers.</p>"},{"location":"api-reference/#database-connection","title":"Database Connection","text":""},{"location":"api-reference/#pyzot.db.ZoteroDatabase","title":"<code>pyzot.db.ZoteroDatabase</code>","text":"<p>Context-manager wrapper around a read-only sqlite3 connection.</p> Source code in <code>src/pyzot/db.py</code> <pre><code>class ZoteroDatabase:\n    \"\"\"Context-manager wrapper around a read-only sqlite3 connection.\"\"\"\n\n    def __init__(self, path: Path | str | None = None, warn_if_open: bool = True):\n        if path is None:\n            path = discover_db()\n        self.path = Path(path)\n\n        if warn_if_open and _check_wal_lock(self.path):\n            import warnings\n\n            warnings.warn(\n                f\"Zotero appears to be running (WAL journal found at {self.path}-wal). \"\n                \"Changes made in Zotero may not be visible until it closes.\",\n                UserWarning,\n                stacklevel=2,\n            )\n\n        uri = f\"file:{self.path}?mode=ro\"\n        self._conn = sqlite3.connect(uri, uri=True, check_same_thread=False)\n        self._conn.row_factory = sqlite3.Row\n        self._conn.execute(\"PRAGMA query_only = ON\")\n\n        self.schema_version = _check_schema_version(self._conn)\n\n    # ------------------------------------------------------------------\n    # Low-level helpers\n    # ------------------------------------------------------------------\n\n    def execute(self, sql: str, params: tuple = ()) -&gt; sqlite3.Cursor:\n        return self._conn.execute(sql, params)\n\n    def fetchall(self, sql: str, params: tuple = ()) -&gt; list[sqlite3.Row]:\n        return self._conn.execute(sql, params).fetchall()\n\n    def fetchone(self, sql: str, params: tuple = ()) -&gt; sqlite3.Row | None:\n        return self._conn.execute(sql, params).fetchone()\n\n    def close(self) -&gt; None:\n        self._conn.close()\n\n    # ------------------------------------------------------------------\n    # Context manager\n    # ------------------------------------------------------------------\n\n    def __enter__(self) -&gt; ZoteroDatabase:\n        return self\n\n    def __exit__(self, *_: object) -&gt; None:\n        self.close()\n\n    def __repr__(self) -&gt; str:\n        return f\"ZoteroDatabase(path={self.path!r}, schema_version={self.schema_version})\"\n</code></pre>"},{"location":"api-reference/#queries-search","title":"Queries: Search","text":""},{"location":"api-reference/#pyzot.queries.search","title":"<code>pyzot.queries.search</code>","text":"<p>Search queries \u2014 field-level and full-text.</p>"},{"location":"api-reference/#pyzot.queries.search.get_item_fulltext","title":"<code>get_item_fulltext(db, item_id_or_key)</code>","text":"<p>Return indexed full-text token content (bag-of-words) with metadata fallback.</p> Source code in <code>src/pyzot/queries/search.py</code> <pre><code>def get_item_fulltext(db: ZoteroDatabase, item_id_or_key: int | str) -&gt; str | None:\n    \"\"\"Return indexed full-text token content (bag-of-words) with metadata fallback.\"\"\"\n    text, _ = get_item_fulltext_with_strategy(db, item_id_or_key, prefer_network=False)\n    return text\n</code></pre>"},{"location":"api-reference/#pyzot.queries.search.get_item_fulltext_with_strategy","title":"<code>get_item_fulltext_with_strategy(db, item_id_or_key, *, prefer_network=True, auth=None)</code>","text":"<p>Return full-text plus the source used to retrieve it.</p> <p>Strategy order: 1) <code>.zotero-ft-cache</code> file of any attached PDF (cheapest, most reliable) 2) direct network access (institution/network-location access) 3) config-based auth credentials 4) item metadata fallback</p> Source code in <code>src/pyzot/queries/search.py</code> <pre><code>def get_item_fulltext_with_strategy(\n    db: ZoteroDatabase,\n    item_id_or_key: int | str,\n    *,\n    prefer_network: bool = True,\n    auth: dict[str, str] | None = None,\n) -&gt; tuple[str | None, str]:\n    \"\"\"Return full-text plus the source used to retrieve it.\n\n    Strategy order:\n    1) ``.zotero-ft-cache`` file of any attached PDF (cheapest, most reliable)\n    2) direct network access (institution/network-location access)\n    3) config-based auth credentials\n    4) item metadata fallback\n    \"\"\"\n    item = get_item(db, item_id_or_key)\n    if item is None:\n        return None, \"not_found\"\n\n    # 1) Local Zotero full-text cache (.zotero-ft-cache) for any attachment\n    cached = _read_local_fulltext_cache(item, db.path.parent)\n    if cached:\n        return cached, \"cache\"\n\n    if prefer_network:\n        urls = _candidate_fulltext_urls(item)\n        for url in urls:\n            text = _fetch_url_text(url)\n            if text:\n                return text, \"network\"\n\n        auth_cfg = auth or {}\n        user = (auth_cfg.get(\"username\") or \"\").strip()\n        password = auth_cfg.get(\"password\") or \"\"\n        if user and password:\n            for url in urls:\n                text = _fetch_url_text(url, username=user, password=password)\n                if text:\n                    return text, \"config_auth\"\n\n    fallback_parts = [item.title]\n    abstract = item.fields.get(\"abstractNote\") or item.fields.get(\"abstract\")\n    if abstract:\n        fallback_parts.append(abstract)\n    fallback_parts.extend(note.plain_text for note in item.notes if note.plain_text)\n    text = \"\\n\\n\".join(part for part in fallback_parts if part).strip()\n    if text:\n        return text, \"metadata\"\n    return None, \"none\"\n</code></pre>"},{"location":"api-reference/#pyzot.queries.search.search_by_author","title":"<code>search_by_author(db, query)</code>","text":"<p>Search items by creator first or last name (case-insensitive, partial match).</p> Source code in <code>src/pyzot/queries/search.py</code> <pre><code>def search_by_author(db: ZoteroDatabase, query: str) -&gt; list[Item]:\n    \"\"\"Search items by creator first or last name (case-insensitive, partial match).\"\"\"\n    like = f\"%{query}%\"\n    rows = db.fetchall(\n        \"\"\"\n        SELECT DISTINCT ic.itemID\n        FROM itemCreators ic\n        JOIN creators c ON ic.creatorID = c.creatorID\n        WHERE c.lastName LIKE ? OR c.firstName LIKE ?\n        \"\"\",\n        (like, like),\n    )\n    return _build_items(db, [r[\"itemID\"] for r in rows])\n</code></pre>"},{"location":"api-reference/#pyzot.queries.search.search_fulltext","title":"<code>search_fulltext(db, query)</code>","text":"<p>Search the full-text index (fulltextWords table).</p> Source code in <code>src/pyzot/queries/search.py</code> <pre><code>def search_fulltext(db: ZoteroDatabase, query: str) -&gt; list[Item]:\n    \"\"\"Search the full-text index (fulltextWords table).\"\"\"\n    words = query.lower().split()\n    if not words:\n        return []\n\n    # Each word must appear in the same item\n    word_subqueries = \" AND \".join(\n        f\"EXISTS (SELECT 1 FROM fulltextWords fw{i} JOIN fulltextItems fi{i} \"\n        f\"ON fw{i}.wordID = fi{i}.wordID WHERE fi{i}.itemID = i.itemID \"\n        f\"AND fw{i}.word LIKE ?)\"\n        for i in range(len(words))\n    )\n    params = tuple(f\"%{w}%\" for w in words)\n\n    sql = f\"\"\"\n        SELECT DISTINCT i.itemID\n        FROM items i\n        WHERE i.itemID NOT IN (SELECT itemID FROM itemNotes)\n          AND i.itemID NOT IN (SELECT itemID FROM itemAttachments)\n          AND {word_subqueries}\n        ORDER BY i.dateAdded DESC\n    \"\"\"\n    try:\n        rows = db.fetchall(sql, params)\n        return _build_items(db, [r[\"itemID\"] for r in rows])\n    except Exception:\n        # fulltextWords may not be populated; fall back to field search\n        return search_items(db, query)\n</code></pre>"},{"location":"api-reference/#pyzot.queries.search.search_items","title":"<code>search_items(db, query, fields=None, item_type=None)</code>","text":"<p>Search item field values. If fields is None, search all fields.</p> Source code in <code>src/pyzot/queries/search.py</code> <pre><code>def search_items(\n    db: ZoteroDatabase,\n    query: str,\n    fields: list[str] | None = None,\n    item_type: str | None = None,\n) -&gt; list[Item]:\n    \"\"\"Search item field values. If fields is None, search all fields.\"\"\"\n    like = f\"%{query}%\"\n\n    if fields:\n        placeholders = \",\".join(\"?\" * len(fields))\n        field_clause = f\"AND f.fieldName IN ({placeholders})\"\n        field_params: tuple = tuple(fields)\n    else:\n        field_clause = \"\"\n        field_params = ()\n\n    type_clause = \"\"\n    type_params: tuple = ()\n    if item_type:\n        type_clause = \"AND it.typeName = ?\"\n        type_params = (item_type,)\n\n    sql = f\"\"\"\n        SELECT DISTINCT i.itemID\n        FROM items i\n        JOIN itemTypes it ON i.itemTypeID = it.itemTypeID\n        LEFT JOIN itemData id ON i.itemID = id.itemID\n        LEFT JOIN fields f ON id.fieldID = f.fieldID\n        LEFT JOIN itemDataValues idv ON id.valueID = idv.valueID\n        WHERE i.itemID NOT IN (SELECT itemID FROM itemNotes)\n          AND i.itemID NOT IN (SELECT itemID FROM itemAttachments)\n          AND idv.value LIKE ?\n          {field_clause}\n          {type_clause}\n        ORDER BY i.dateAdded DESC\n    \"\"\"\n    params = (like,) + field_params + type_params\n    rows = db.fetchall(sql, params)\n    return _build_items(db, [r[\"itemID\"] for r in rows])\n</code></pre>"},{"location":"api-reference/#queries-items","title":"Queries: Items","text":""},{"location":"api-reference/#pyzot.queries.items","title":"<code>pyzot.queries.items</code>","text":"<p>Item queries \u2014 the core denormalised fetch.</p>"},{"location":"api-reference/#pyzot.queries.items.get_top_level_items","title":"<code>get_top_level_items(db, library_id=None)</code>","text":"<p>Items not in any collection (top-level unfiled items).</p> Source code in <code>src/pyzot/queries/items.py</code> <pre><code>def get_top_level_items(db: ZoteroDatabase, library_id: int | None = None) -&gt; list[Item]:\n    \"\"\"Items not in any collection (top-level unfiled items).\"\"\"\n    sql = \"\"\"\n        SELECT i.itemID FROM items i\n        WHERE i.itemID NOT IN (SELECT itemID FROM itemNotes)\n          AND i.itemID NOT IN (SELECT itemID FROM itemAttachments)\n          AND i.itemID NOT IN (SELECT itemID FROM collectionItems)\n    \"\"\"\n    params: list = []\n    if library_id is not None:\n        sql += \" AND i.libraryID = ?\"\n        params.append(library_id)\n    rows = db.fetchall(sql, tuple(params))\n    return _build_items(db, [r[\"itemID\"] for r in rows])\n</code></pre>"},{"location":"api-reference/#queries-collections","title":"Queries: Collections","text":""},{"location":"api-reference/#pyzot.queries.collections","title":"<code>pyzot.queries.collections</code>","text":"<p>Collection queries.</p>"},{"location":"api-reference/#pyzot.queries.collections.get_collection_tree","title":"<code>get_collection_tree(db, library_id=None)</code>","text":"<p>Return root-level collections with children nested recursively.</p> Source code in <code>src/pyzot/queries/collections.py</code> <pre><code>def get_collection_tree(db: ZoteroDatabase, library_id: int | None = None) -&gt; list[Collection]:\n    \"\"\"Return root-level collections with children nested recursively.\"\"\"\n    all_cols = get_all_collections(db, library_id)\n    by_id = {c.collection_id: c for c in all_cols}\n    roots: list[Collection] = []\n    for c in all_cols:\n        if c.parent_collection_id is None:\n            roots.append(c)\n        elif c.parent_collection_id in by_id:\n            by_id[c.parent_collection_id].children.append(c)\n    return roots\n</code></pre>"},{"location":"api-reference/#queries-attachments","title":"Queries: Attachments","text":""},{"location":"api-reference/#pyzot.queries.attachments","title":"<code>pyzot.queries.attachments</code>","text":"<p>Attachment queries and path resolution.</p>"},{"location":"api-reference/#pyzot.queries.attachments.enrich_attachment_paths","title":"<code>enrich_attachment_paths(attachments, data_dir)</code>","text":"<p>Resolve and check existence of attachment paths in place.</p> Source code in <code>src/pyzot/queries/attachments.py</code> <pre><code>def enrich_attachment_paths(attachments: list[Attachment], data_dir: Path) -&gt; list[Attachment]:\n    \"\"\"Resolve and check existence of attachment paths in place.\"\"\"\n    for att in attachments:\n        p = resolve_attachment_path(att, data_dir)\n        att.absolute_path = p\n        att.file_exists = p is not None and p.exists()\n    return attachments\n</code></pre>"},{"location":"api-reference/#pyzot.queries.attachments.get_all_pdfs","title":"<code>get_all_pdfs(db, data_dir)</code>","text":"<p>Return all (item, attachment) pairs where the attachment is a PDF.</p> Source code in <code>src/pyzot/queries/attachments.py</code> <pre><code>def get_all_pdfs(db: ZoteroDatabase, data_dir: Path) -&gt; list[tuple[Item, Attachment]]:\n    \"\"\"Return all (item, attachment) pairs where the attachment is a PDF.\"\"\"\n    rows = db.fetchall(\n        \"\"\"\n        SELECT ia.parentItemID, i.itemID, i.key, ia.linkMode,\n               ia.contentType, ia.path\n        FROM itemAttachments ia\n        JOIN items i ON ia.itemID = i.itemID\n        WHERE LOWER(ia.contentType) = 'application/pdf'\n          AND ia.parentItemID IS NOT NULL\n        \"\"\"\n    )\n    # Group by parent item\n    from collections import defaultdict\n\n    parent_ids: list[int] = []\n    att_by_parent: dict[int, list[Attachment]] = defaultdict(list)\n    for r in rows:\n        parent_id = r[\"parentItemID\"]\n        att = Attachment(\n            item_id=r[\"itemID\"],\n            key=r[\"key\"],\n            parent_item_id=parent_id,\n            link_mode=r[\"linkMode\"] if r[\"linkMode\"] is not None else 0,\n            content_type=r[\"contentType\"] or \"\",\n            path=r[\"path\"],\n        )\n        p = resolve_attachment_path(att, data_dir)\n        att.absolute_path = p\n        att.file_exists = p is not None and p.exists()\n        att_by_parent[parent_id].append(att)\n        if parent_id not in parent_ids:\n            parent_ids.append(parent_id)\n\n    items = {item.item_id: item for item in _build_items(db, parent_ids)}\n    result: list[tuple[Item, Attachment]] = []\n    for pid in parent_ids:\n        item = items.get(pid)\n        if item:\n            for att in att_by_parent[pid]:\n                result.append((item, att))\n    return result\n</code></pre>"},{"location":"api-reference/#pyzot.queries.attachments.resolve_attachment_path","title":"<code>resolve_attachment_path(attachment, data_dir)</code>","text":"<p>Resolve the absolute path of an attachment file.</p> Source code in <code>src/pyzot/queries/attachments.py</code> <pre><code>def resolve_attachment_path(attachment: Attachment, data_dir: Path) -&gt; Path | None:\n    \"\"\"Resolve the absolute path of an attachment file.\"\"\"\n    if attachment.link_mode == 0:  # imported_file: storage/{key}/{filename}\n        if attachment.path:\n            # Zotero stores \"storage:filename\" for imported files\n            filename = attachment.path.removeprefix(\"storage:\")\n            p = data_dir / \"storage\" / attachment.key / filename\n        else:\n            # Try to find any file under the storage key directory\n            storage_dir = data_dir / \"storage\" / attachment.key\n            if storage_dir.exists():\n                files = list(storage_dir.iterdir())\n                return files[0] if files else None\n            return None\n        return p\n    elif attachment.link_mode == 2:  # linked_file: absolute path\n        if attachment.path:\n            return Path(attachment.path)\n    elif attachment.link_mode == 1:  # imported_url: same as imported_file\n        if attachment.path:\n            filename = attachment.path.removeprefix(\"storage:\")\n            return data_dir / \"storage\" / attachment.key / filename\n    # linked_url (3) has no local file\n    return None\n</code></pre>"},{"location":"architecture-write/","title":"Write-Path Architecture","text":"<p><code>pyzot</code> never creates Zotero parent items by writing directly to <code>zotero.sqlite</code>. Item creation goes through Zotero's local connector server at <code>127.0.0.1:23119</code>.</p>"},{"location":"architecture-write/#flow","title":"Flow","text":"<pre><code>zot add &lt;input&gt;\n  -&gt; detect DOI / arXiv / PMID / ISBN / URL / citation / file / import\n  -&gt; resolve metadata when needed\n  -&gt; convert CSL-JSON to connector item shape\n  -&gt; POST to Zotero connector\n  -&gt; update connector session for target collection and tags\n</code></pre> <p>Endpoints used:</p> Endpoint Purpose <code>GET /connector/ping</code> Liveness check <code>GET /connector/getSelectedCollection</code> Current Zotero target <code>POST /connector/saveItems</code> Save metadata items <code>POST /connector/saveSnapshot</code> Save generic webpages <code>POST /connector/saveStandaloneAttachment</code> Import local PDF/EPUB <code>POST /connector/import</code> Import RIS/BibTeX/CSL-JSON <code>POST /connector/updateSession</code> Apply collection and tags"},{"location":"architecture-write/#kept-inputs","title":"Kept Inputs","text":"Source Path DOI Crossref arXiv arXiv Atom feed PubMed PMID NCBI eUtils ISBN OpenLibrary Citation string Crossref search, then OpenAlex, then Semantic Scholar URL arXiv/PubMed/DOI pattern routing, otherwise connector snapshot Local PDF/EPUB Connector standalone attachment RIS/BibTeX/CSL-JSON Connector import"},{"location":"architecture-write/#removed-scope","title":"Removed Scope","text":"<p>The Tier-B scrub removed publisher-specific PDF scraping, service login flows, cookie/profile management, and automatic open-access PDF lookup. Users can still attach a local file directly, and <code>zot attachments fetch</code> keeps a plain HTTP resolver path for existing items.</p>"},{"location":"architecture-write/#data-directory","title":"Data Directory","text":"<p><code>pyzot_home()</code> resolves:</p> <ol> <li><code>PYZOT_HOME</code>, if set.</li> <li>A parent directory containing <code>pyproject.toml</code>, using <code>&lt;that-dir&gt;/.pyzot/</code>.</li> <li><code>~/.pyzot</code>.</li> </ol> <p>The directory is created lazily for config, cache, sessions, and logs.</p>"},{"location":"cli-reference/","title":"CLI Reference","text":"<p>The <code>pyzot</code> provides a robust set of terminal commands under the <code>zot</code> binary. </p> <p>This reference is automatically generated from the application's Click source code using <code>mkdocs-click</code>.</p>"},{"location":"cli-reference/#zot","title":"zot","text":"<p>zot \u2014 CLI for your Zotero library.</p> <p>Usage:</p> <pre><code>zot [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --db TEXT                       Path to zotero.sqlite\n  --library INTEGER               Library ID (default: 1)\n  --format [table|json|csv]       Output format\n  --no-color                      Disable colour output\n  --allow-write                   Allow write operations (ad-hoc override; or\n                                  set write.enabled=true in config).\n  --connector-url TEXT            Zotero connector URL (default:\n                                  http://127.0.0.1:23119).\n  --require-zotero / --no-require-zotero\n                                  Fail if Zotero is not reachable (default:\n                                  require).\n  --help                          Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#add","title":"add","text":"<p>Add items to your Zotero library via the connector.</p> <p>When called with a single positional argument (no subcommand), automatically detects what kind of input it is (DOI, arXiv ID, PMID, ISBN, URL, citation string, or local file path) and dispatches to the right handler.</p> <p>Examples:</p> <pre><code>zot add \"10.1109/TPWRS.2023.1234567\"        # auto-detect DOI\nzot add \"2401.12345\"                        # auto-detect arXiv\nzot add \"Zhang, J. et al. (2025) Beyond...\"  # auto-detect citation\nzot add \"/home/me/paper.pdf\"                # auto-detect file\nzot add batch papers.txt                    # batch mode\n</code></pre> <p>Use <code>zot add batch</code> for scripted multi-item imports.</p> <p>Usage:</p> <pre><code>zot add [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#batch","title":"batch","text":"<p>Process a file of inputs, one per line.</p> <p>FILE may be <code>-</code> to read from stdin.  Each line is passed through the same auto-detect dispatcher as <code>zot add \"&lt;anything&gt;\"</code>.</p> <p>Lines that start with <code>#</code> or are blank are silently skipped.</p> <p>After processing all lines a summary table is printed and, if any lines failed, the command exits with code 1.</p> <p>Note: <code>--jobs N</code> (N &gt; 1) is reserved for parallel resolver lookups in a future release.  Currently it is accepted but has no effect.</p> <p>Example:</p> <pre><code>zot add batch papers.txt --collection \"Smart Grid\"\ncat dois.txt | zot add batch - --tag imported\n</code></pre> <p>Usage:</p> <pre><code>zot add batch [OPTIONS] FILE\n</code></pre> <p>Options:</p> <pre><code>  -c, --collection NAME           Collection name applied to all items.\n  -t, --tag TEXT                  Tag to apply to all items (repeatable).\n  --dry-run                       Resolve and print what would be submitted\n                                  without making connector calls.\n  --on-duplicate [report|skip|force-add]\n                                  Behaviour when a duplicate is found.\n                                  [default: report]\n  -v, --verbose                   Print verbose HTTP request/response info.\n  --non-interactive               Never prompt; fail/skip ambiguous citations.\n  --jobs INTEGER                  (Stub \u2014 reserved for parallel resolver\n                                  lookups in v0.3.0; currently no-op.)\n                                  [default: 1]\n  --help                          Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#cite","title":"cite","text":"<p>Add one or more items from free-text citation strings.</p> <p>Usage:</p> <pre><code>zot add cite [OPTIONS] [CITATION_TEXT]\n</code></pre> <p>Options:</p> <pre><code>  -f, --file PATH\n  --threshold INTEGER             [default: 50]\n  --gap FLOAT                     [default: 1.4]\n  -c, --collection NAME           Collection name to add the item to.\n  -t, --tag TEXT                  Tag to apply (repeatable).\n  --dry-run                       Print the JSON that would be POSTed without\n                                  making any request to the connector.\n  --on-duplicate [report|skip|force-add]\n                                  Behaviour when a duplicate is found.\n                                  [default: report]\n  -v, --verbose                   Print verbose HTTP request/response info.\n  --non-interactive               Never prompt for citation disambiguation.\n  --help                          Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#status","title":"status","text":"<p>Check whether Zotero is running and report the current target.</p> <p>Prints reachability, selected collection, connector URL, and a hint about enabling write capability if not already enabled.</p> <p>Usage:</p> <pre><code>zot add status [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#attachments","title":"attachments","text":"<p>Browse and open Zotero attachments.</p> <p>Usage:</p> <pre><code>zot attachments [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#add_1","title":"add","text":"<p>Attach an existing local file to an existing Zotero item.</p> <p>Inserts the attachment directly into <code>zotero.sqlite</code> and copies the file into <code>&lt;zotero-data-dir&gt;/storage/&lt;new-key&gt;/</code>.</p> <p>Example:</p> <pre><code>zot attachments add ABCD1234 ~/Downloads/paper.pdf\n</code></pre> <p>Usage:</p> <pre><code>zot attachments add [OPTIONS] PARENT_KEY FILE_PATH\n</code></pre> <p>Options:</p> <pre><code>  --title TEXT       Display title (default: filename stem).\n  --source-url TEXT  Source URL for provenance.\n  --help             Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#fetch","title":"fetch","text":"<p>Find and attach a PDF for an existing item using HTTP resolvers.</p> <p>Tries DOI, item URL, and any custom resolvers in turn. The first successful PDF is attached as a child of the item.</p> <p>Example:</p> <pre><code>zot attachments fetch 5UFZMSLU\n</code></pre> <p>Usage:</p> <pre><code>zot attachments fetch [OPTIONS] PARENT_KEY\n</code></pre> <p>Options:</p> <pre><code>  --methods TEXT  Comma-separated subset of resolvers to enable.  [default:\n                  doi,url,custom]\n  --help          Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#fetch-all","title":"fetch-all","text":"<p>Find PDFs for every library item that has a DOI/URL but no PDF.</p> <p>Long-running. Honour --limit while testing. Items already with a PDF attachment are skipped. Rate-limiting is enforced per-domain by the pipeline internally.</p> <p>Example:</p> <pre><code>zot attachments fetch-all --limit 20\n</code></pre> <p>Usage:</p> <pre><code>zot attachments fetch-all [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --methods TEXT   [default: doi,url,custom]\n  --limit INTEGER  Cap total items processed.\n  --help           Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#fetch-collection","title":"fetch-collection","text":"<p>Find PDFs for every item in a collection that doesn't already have one.</p> <p>Example:</p> <pre><code>zot attachments fetch-collection \"[Paper] LV_UG_Cable_Models_DSSE\"\n</code></pre> <p>Usage:</p> <pre><code>zot attachments fetch-collection [OPTIONS] COLLECTION_NAME\n</code></pre> <p>Options:</p> <pre><code>  --methods TEXT      [default: doi,url,custom]\n  --include-with-pdf  Also process items that already have a PDF (re-fetch).\n  --help              Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#list","title":"list","text":"<p>List all attachments.</p> <p>Usage:</p> <pre><code>zot attachments list [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --missing    Only show missing files\n  --type TEXT  Filter by content type (e.g. pdf)\n  --help       Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#open","title":"open","text":"<p>Open the first attachment with the system default application.</p> <p>Usage:</p> <pre><code>zot attachments open [OPTIONS] ID_OR_KEY\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#path","title":"path","text":"<p>Print the resolved absolute path(s) for an item's attachments.</p> <p>Usage:</p> <pre><code>zot attachments path [OPTIONS] ID_OR_KEY\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#collections","title":"collections","text":"<p>Browse Zotero collections.</p> <p>Usage:</p> <pre><code>zot collections [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#assign","title":"assign","text":"<p>Assign an existing item to a collection.</p> <p>Does not remove the item from any existing collections (additive only).</p> <p>Examples:</p> <pre><code>zot collection assign AB3CD7EF \"Smart Grid\"\nzot collection assign AB3CD7EF \"[Paper] LV_UG_Cable_Models_DSSE\"\n</code></pre> <p>Usage:</p> <pre><code>zot collections assign [OPTIONS] ITEM_KEY COLLECTION_NAME\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#items","title":"items","text":"<p>List items in a collection.</p> <p>Usage:</p> <pre><code>zot collections items [OPTIONS] ID_OR_NAME\n</code></pre> <p>Options:</p> <pre><code>  -r, --recursive  Include sub-collections\n  --type TEXT      Filter to item type (e.g. journalArticle)\n  --help           Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#list_1","title":"list","text":"<p>Print the collection tree.</p> <p>Usage:</p> <pre><code>zot collections list [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --flat  Flat list with IDs instead of tree view\n  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#show","title":"show","text":"<p>Show details for a collection.</p> <p>Usage:</p> <pre><code>zot collections show [OPTIONS] ID_OR_NAME\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#config","title":"config","text":"<p>Manage pyzot settings.</p> <p>Usage:</p> <pre><code>zot config [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#get","title":"get","text":"<p>Print the value of KEY (section.key form, e.g. write.enabled).</p> <p>Exits with code 1 if the key is not set.</p> <p>Usage:</p> <pre><code>zot config get [OPTIONS] KEY\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#library-auth","title":"library-auth","text":"<p>Store or display per-library authentication details for full-text retrieval.</p> <p>Usage:</p> <pre><code>zot config library-auth [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --library INTEGER   Library ID (default: current)\n  --institution TEXT  Institution/library provider name\n  --username TEXT     Library login username\n  --password TEXT     Library login password/token\n  --show              Show current authentication details\n  --help              Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#path_1","title":"path","text":"<p>Print the pyzot home directory path.</p> <p>Usage:</p> <pre><code>zot config path [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#set","title":"set","text":"<p>Set KEY to VALUE and persist to /config.toml. <p>Supported keys include write.enabled, write.connector_url, and any other dotted section.key value.</p> <p>Usage:</p> <pre><code>zot config set [OPTIONS] KEY VALUE\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#export","title":"export","text":"<p>Export items to various formats.</p> <p>Usage:</p> <pre><code>zot export [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#bib","title":"bib","text":"<p>Export items as BibTeX.</p> <p>Usage:</p> <pre><code>zot export bib [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  -c, --collection TEXT  Export a specific collection\n  -i, --item TEXT        Export a specific item by ID or Key\n  --all                  Export entire library\n  -o, --output TEXT      Output file (default: stdout)\n  --help                 Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#csv","title":"csv","text":"<p>Export items as CSV.</p> <p>Usage:</p> <pre><code>zot export csv [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  -c, --collection TEXT  Export a specific collection\n  -i, --item TEXT        Export a specific item by ID or Key\n  --all                  Export entire library\n  -o, --output TEXT      Output file (default: stdout)\n  --help                 Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#json","title":"json","text":"<p>Export items as JSON.</p> <p>Usage:</p> <pre><code>zot export json [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  -c, --collection TEXT  Export a specific collection\n  -i, --item TEXT        Export a specific item by ID or Key\n  --all                  Export entire library\n  -o, --output TEXT      Output file (default: stdout)\n  --help                 Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#markdown","title":"markdown","text":"<p>Export items as a Markdown report.</p> <p>Usage:</p> <pre><code>zot export markdown [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  -c, --collection TEXT  Export a specific collection\n  -i, --item TEXT        Export a specific item by ID or Key\n  --all                  Export entire library\n  -o, --output TEXT      Output file (default: stdout)\n  --notes                Include notes sections\n  --help                 Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#items_1","title":"items","text":"<p>Browse Zotero items.</p> <p>Usage:</p> <pre><code>zot items [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#attachments_1","title":"attachments","text":"<p>List attachments for an item.</p> <p>Usage:</p> <pre><code>zot items attachments [OPTIONS] ID_OR_KEY\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#fulltext","title":"fulltext","text":"<p>Retrieve full text for an item with network/auth/local fallback.</p> <p>Usage:</p> <pre><code>zot items fulltext [OPTIONS] ID_OR_KEY\n</code></pre> <p>Options:</p> <pre><code>  --max-chars INTEGER  Maximum number of characters to print  [default: 10000]\n  --offline            Skip network/auth retrieval and only use local Zotero\n                       data\n  --help               Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#list_2","title":"list","text":"<p>List items (paginated).</p> <p>Usage:</p> <pre><code>zot items list [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --type TEXT        Filter by item type\n  --collection TEXT  Filter by collection name/ID\n  --limit INTEGER    Max items to show  [default: 50]\n  --help             Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#notes","title":"notes","text":"<p>Show notes attached to an item.</p> <p>Usage:</p> <pre><code>zot items notes [OPTIONS] ID_OR_KEY\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#show_1","title":"show","text":"<p>Show full details for an item.</p> <p>Usage:</p> <pre><code>zot items show [OPTIONS] ID_OR_KEY\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#search","title":"search","text":"<p>Search items across fields, authors, tags, DOIs, or years.</p> <p>Examples:   zot search \"bayesian\" --field title   zot search --author \"Numair\"   zot search \"power flow\" --type journalArticle   zot search --doi 10.1038/example   zot search --tag \"machine-learning\"   zot search --year 2020-2024</p> <p>Usage:</p> <pre><code>zot search [OPTIONS] [QUERY]\n</code></pre> <p>Options:</p> <pre><code>  --field TEXT   Restrict search to field(s) e.g. title, abstract\n  --author TEXT  Search by author name (first or last, partial match)\n  --type TEXT    Restrict to item type\n  --doi TEXT     Search by DOI\n  --tag TEXT     Search by tag\n  --year TEXT    Year range e.g. 2020-2024 or 2022\n  --fulltext     Use full-text index instead of field search\n  --help         Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#stats","title":"stats","text":"<p>Library statistics dashboard.</p> <p>Usage:</p> <pre><code>zot stats [OPTIONS] [COMMAND] [ARGS]...\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#collections_1","title":"collections","text":"<p>Items per collection (top N).</p> <p>Usage:</p> <pre><code>zot stats collections [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --top INTEGER  [default: 20]\n  --help         Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#summary","title":"summary","text":"<p>Overall counts.</p> <p>Usage:</p> <pre><code>zot stats summary [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#tags","title":"tags","text":"<p>Top N tags by frequency.</p> <p>Usage:</p> <pre><code>zot stats tags [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --top INTEGER  Number of top tags to show  [default: 20]\n  --help         Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#types","title":"types","text":"<p>Items per item type.</p> <p>Usage:</p> <pre><code>zot stats types [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"cli-reference/#years","title":"years","text":"<p>Publication year histogram.</p> <p>Usage:</p> <pre><code>zot stats years [OPTIONS]\n</code></pre> <p>Options:</p> <pre><code>  --help  Show this message and exit.\n</code></pre>"},{"location":"commands/","title":"<code>zot</code> Command Reference","text":"<p>Global options go before the subcommand:</p> <pre><code>zot [--db PATH] [--library ID] [--format table|json|csv] [--no-color] &lt;command&gt;\n</code></pre> <p>Write-related globals:</p> <pre><code>zot [--allow-write] [--connector-url URL] [--require-zotero/--no-require-zotero] &lt;command&gt;\n</code></pre>"},{"location":"commands/#read-commands","title":"Read Commands","text":"<pre><code>zot stats [summary|types|tags|years|collections]\nzot collections list [--flat]\nzot collections show &lt;id|name&gt;\nzot collections items &lt;id|name&gt; [--recursive] [--type TYPE]\nzot items list [--type TYPE] [--collection NAME] [--limit N]\nzot items show &lt;id|key&gt;\nzot items attachments &lt;id|key&gt;\nzot items notes &lt;id|key&gt;\nzot items fulltext &lt;id|key&gt; [--offline]\nzot search \"query\" [--field FIELD] [--type TYPE]\nzot search --author \"Name\"\nzot search --doi 10.xxxx/yyyy\nzot search --tag \"tag-name\"\nzot search --year 2020-2024\nzot search \"query\" --fulltext\nzot attachments list [--missing] [--type pdf]\nzot attachments path &lt;id|key&gt;\nzot attachments open &lt;id|key&gt;\nzot export json|csv|bib|markdown --collection \"Name\" [--output file]\nzot export json|csv|bib|markdown --all [--output file]\n</code></pre>"},{"location":"commands/#config","title":"Config","text":"<pre><code>zot config path\nzot config get write.enabled\nzot config set write.enabled true\nzot config set write.connector_url http://127.0.0.1:23119\n</code></pre> <p>Config is stored under <code>&lt;pyzot-home&gt;/config.toml</code>. Use <code>zot config path</code> to see the active directory.</p>"},{"location":"commands/#add-items","title":"Add Items","text":"<p>Writes require <code>write.enabled=true</code>, <code>--allow-write</code>, or <code>PYZOT_ALLOW_WRITE=1</code>. Zotero must be running unless <code>--dry-run</code> is used.</p> <pre><code>zot add status\nzot add &lt;input&gt; [--collection NAME] [--tag TEXT] [--dry-run]\nzot add batch &lt;file|-&gt;\n</code></pre> <p><code>zot add &lt;input&gt;</code> auto-detects:</p> Input Example DOI <code>zot add \"10.1038/s41586-020-2649-2\"</code> arXiv ID <code>zot add \"2401.12345\"</code> PMID <code>zot add \"31452104\"</code> ISBN <code>zot add \"9780262033848\"</code> URL <code>zot add \"https://arxiv.org/abs/1706.03762\"</code> Citation <code>zot add \"Smith, J. (2020) My Paper. Nature\"</code> Local PDF/EPUB <code>zot add ~/Downloads/paper.pdf</code> RIS/BibTeX/CSL-JSON <code>zot add ~/Downloads/refs.bib</code> <p>For citation-file input and advanced citation matching options, use:</p> <pre><code>zot add cite \"&lt;citation&gt;\"\nzot add cite --file refs.txt --threshold 60 --gap 1.2\n</code></pre> <p>Older explicit forms such as <code>zot add doi &lt;DOI&gt;</code> and <code>zot add file &lt;PATH&gt;</code> have been collapsed into <code>zot add &lt;input&gt;</code>.</p>"},{"location":"commands/#attachments","title":"Attachments","text":"<pre><code>zot attachments add &lt;PARENT_KEY&gt; &lt;FILE_PATH&gt; [--title T] [--source-url URL]\nzot attachments fetch &lt;PARENT_KEY&gt; [--methods doi,url,custom]\nzot attachments fetch-collection &lt;NAME&gt; [--include-with-pdf] [--methods doi,url,custom]\nzot attachments fetch-all [--limit N] [--methods doi,url,custom]\n</code></pre> <p><code>attachments fetch</code> is HTTP-only. It tries DOI landing pages, item URLs, and custom resolver URLs, then attaches the first downloaded PDF/EPUB.</p>"},{"location":"data-models/","title":"Data Models","text":"<p>The following Pydantic models represent the core data structures returned by the <code>pyzot.queries</code> modules.</p>"},{"location":"data-models/#pyzot.models.Item","title":"<code>pyzot.models.Item</code>","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>src/pyzot/models.py</code> <pre><code>class Item(BaseModel):\n    item_id: int\n    key: str\n    item_type: str  # journalArticle, book, conferencePaper, \u2026\n    library_id: int\n    date_added: datetime\n    date_modified: datetime\n    fields: dict[str, str] = {}\n    creators: list[Creator] = []\n    tags: list[str] = []\n    collections: list[int] = []\n    attachments: list[Attachment] = []\n    notes: list[Note] = []\n\n    model_config = {\"arbitrary_types_allowed\": True}\n\n    # ------------------------------------------------------------------\n    # Convenience properties\n    # ------------------------------------------------------------------\n\n    @property\n    def title(self) -&gt; str:\n        return self.fields.get(\"title\", \"(no title)\")\n\n    @property\n    def year(self) -&gt; str | None:\n        date = self.fields.get(\"date\", \"\")\n        if date:\n            m = re.search(r\"\\b(\\d{4})\\b\", date)\n            if m:\n                return m.group(1)\n        return None\n\n    @property\n    def doi(self) -&gt; str | None:\n        return self.fields.get(\"DOI\") or self.fields.get(\"doi\")\n\n    @property\n    def authors(self) -&gt; list[str]:\n        return [\n            c.display_name\n            for c in sorted(self.creators, key=lambda c: c.order_index)\n            if c.creator_type == \"author\"\n        ]\n\n    @property\n    def authors_short(self) -&gt; str:\n        \"\"\"First author last name + et al. for display.\"\"\"\n        a = self.authors\n        if not a:\n            return \"\"\n        if len(a) == 1:\n            return a[0].split(\",\")[0]\n        return a[0].split(\",\")[0] + \" et al.\"\n\n    @property\n    def citation_key(self) -&gt; str | None:\n        # Check standard fields first if any plugin adds them\n        if \"citationKey\" in self.fields:\n            return self.fields[\"citationKey\"]\n        if \"citekey\" in self.fields:\n            return self.fields[\"citekey\"]\n\n        # Check 'extra' field where Better BibTeX stores it\n        extra = self.fields.get(\"extra\", \"\")\n        if extra:\n            for line in extra.splitlines():\n                if line.lower().startswith(\"citation key:\"):\n                    return line.split(\":\", 1)[1].strip()\n        return None\n\n    def __str__(self) -&gt; str:\n        return f\"[{self.item_type}] {self.title} ({self.year or '?'})\"\n</code></pre>"},{"location":"data-models/#pyzot.models.Item.authors_short","title":"<code>authors_short</code>  <code>property</code>","text":"<p>First author last name + et al. for display.</p>"},{"location":"data-models/#pyzot.models.Collection","title":"<code>pyzot.models.Collection</code>","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>src/pyzot/models.py</code> <pre><code>class Collection(BaseModel):\n    collection_id: int\n    key: str\n    name: str\n    parent_collection_id: int | None = None\n    library_id: int\n    children: list[Collection] = []\n    item_count: int = 0\n\n    model_config = {\"arbitrary_types_allowed\": True}\n</code></pre>"},{"location":"data-models/#pyzot.models.Attachment","title":"<code>pyzot.models.Attachment</code>","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>src/pyzot/models.py</code> <pre><code>class Attachment(BaseModel):\n    item_id: int\n    key: str\n    parent_item_id: int\n    link_mode: int  # 0=imported_file, 1=imported_url, 2=linked_file, 3=linked_url\n    content_type: str\n    path: str | None = None\n    absolute_path: Path | None = None\n    file_exists: bool = False\n\n    model_config = {\"arbitrary_types_allowed\": True}\n\n    @property\n    def link_mode_name(self) -&gt; str:\n        return {0: \"imported_file\", 1: \"imported_url\", 2: \"linked_file\", 3: \"linked_url\"}.get(\n            self.link_mode, f\"unknown({self.link_mode})\"\n        )\n\n    @property\n    def filename(self) -&gt; str | None:\n        if self.path:\n            return Path(self.path).name\n        return None\n</code></pre>"},{"location":"data-models/#pyzot.models.Creator","title":"<code>pyzot.models.Creator</code>","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>src/pyzot/models.py</code> <pre><code>class Creator(BaseModel):\n    creator_id: int\n    first_name: str\n    last_name: str\n    creator_type: str  # author, editor, translator, \u2026\n    order_index: int\n\n    @property\n    def display_name(self) -&gt; str:\n        if self.last_name and self.first_name:\n            return f\"{self.last_name}, {self.first_name}\"\n        return self.last_name or self.first_name\n</code></pre>"},{"location":"data-models/#pyzot.models.Note","title":"<code>pyzot.models.Note</code>","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>src/pyzot/models.py</code> <pre><code>class Note(BaseModel):\n    item_id: int\n    parent_item_id: int\n    title: str\n    note: str  # HTML content\n    plain_text: str  # stripped version\n\n    @field_validator(\"plain_text\", mode=\"before\")\n    @classmethod\n    def strip_html(cls, v: str) -&gt; str:\n        if v:\n            return re.sub(r\"&lt;[^&gt;]+&gt;\", \"\", v).strip()\n        return v or \"\"\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<p>This project uses <code>uv</code>. Sync a development environment with all optional features:</p> <pre><code>uv sync --all-extras\n</code></pre> <p>To build and serve the documentation locally, sync the <code>docs</code> extra and run mkdocs:</p> <pre><code>uv sync --extra docs\nuv run mkdocs serve\n</code></pre>"},{"location":"getting-started/#basic-usage-cli","title":"Basic Usage (CLI)","text":"<p>The CLI tool auto-discovers your local Zotero database.</p> <pre><code># View library stats\nzot stats\n\n# Search for papers with \"bayesian\" in the title\nzot search \"bayesian\" --field title\n\n# Get attachment paths for a specific item\nzot attachments path &lt;ITEM_ID_OR_KEY&gt;\n\n# Retrieve full text (network-first, then auth/local fallbacks)\nzot items fulltext &lt;ITEM_ID_OR_KEY&gt;\nzot items fulltext &lt;ITEM_ID_OR_KEY&gt; --offline\n</code></pre>"},{"location":"getting-started/#programmatic-usage-sdk","title":"Programmatic Usage (SDK)","text":"<p><code>pyzot</code> provides a robust Python SDK for custom scripts.</p>"},{"location":"getting-started/#usage-example-search-and-extract-pdf-paths","title":"Usage Example: Search and Extract PDF Paths","text":"<pre><code>from pyzot.db import ZoteroDatabase\nfrom pyzot.queries.search import search_items, search_by_author\n\ndef extract_pdfs():\n    # Use ZoteroDatabase context manager for safe, read-only DB access\n    with ZoteroDatabase() as db:\n        # Search library by title\n        bayesian = search_items(db, \"bayesian\", fields=[\"title\"])\n\n        # Search library by author\n        numair = search_by_author(db, \"Smith\")\n\n        seen = set()\n        for item in bayesian + numair:\n            if item.item_id in seen:\n                continue\n            seen.add(item.item_id)\n\n            # Iterate through attachments and find valid PDFs\n            for att in item.attachments:\n                if att.file_exists and \"pdf\" in att.content_type.lower():\n                    print(f\"{item.key}\\t{att.absolute_path}\")\n\nprint(\"Extracting PDFs...\")\nextract_pdfs()\n</code></pre>"}]}